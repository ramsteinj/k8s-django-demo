apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: yogiyo
  name: postgres-pv-claim
  labels:
    app: yogiyo
spec:
  storageClassName: local-postgres-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi