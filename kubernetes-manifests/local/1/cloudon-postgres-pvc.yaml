apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: cloudon
  name: postgres-pv-claim
  labels:
    app: cloudon
spec:
  storageClassName: local-postgres-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi