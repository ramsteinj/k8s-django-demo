apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: yogiyo
  name: redis-pv-claim
  labels:
    app: yogiyo
spec:
  storageClassName: local-redis-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi